// Single source of truth for the deployed site's canonical origin.
// Import SITE_URL wherever a canonical/sitemap/robots/schema URL is needed
// instead of hardcoding the domain — swapping it here is the only change
// needed if the site ever moves to a different domain.
export const SITE_URL = "https://tayseerdemo.xyz";
